version: '2.1'

services:
  chris47dev:
    build:
      context: .
      args:
        - http_proxy
        - https_proxy
        - no_proxy
    image: hmctsprod.azurecr.io/labs/chris47dev
    ports:
      - $SERVER_PORT:$SERVER_PORT
